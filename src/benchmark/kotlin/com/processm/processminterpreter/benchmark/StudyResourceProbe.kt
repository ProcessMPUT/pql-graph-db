package com.processm.processminterpreter.benchmark

/** One pilot cell used to verify memory and I/O collection before the final resource tasks. */
data class StudyResourceProbe(val dataset: String, val query: String)
